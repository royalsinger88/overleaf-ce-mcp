# overleaf-ce-mcp 快速上手（10 条命令）

适用场景：
- 你已经有自建 Overleaf CE 地址。
- 你在 VPS 上操作，认证文件用 `/root/.olauth`。
- 目标是最快跑通“本地稿件 -> 上传 CE -> 编译验证”。

示例变量（先改成你的）：

```bash
export CE_URL="http://129.150.32.65:17880"
export AUTH_FILE="/root/.olauth"
export WORK_DIR="/root/overleaf-workspace"
export PROJ_DIR="$WORK_DIR/test"
export PROJ_NAME="test"
# 可选：仅当你需要 Semantic Scholar 时设置
export S2_API_KEY="your_semantic_scholar_key"
```

## 1. 安装与启动

```bash
cd /root/overleaf-ce-mcp
/root/miniconda3/bin/python -m venv .venv
source .venv/bin/activate
pip install -U pip
pip install .
```

## 2. 检查环境

```bash
python - <<'PY'
from overleaf_ce_mcp.server import _collect_env_status
print(_collect_env_status())
PY
```

## 3. 校验认证是否可用

```bash
python - <<'PY'
import os
from overleaf_ce_mcp.upload import list_projects
ps = list_projects(ce_url=os.environ["CE_URL"], store_path=os.environ["AUTH_FILE"])
print("projects:", len(ps))
for p in ps[:10]:
    print(p.get("name"), p.get("id"))
PY
```

## 3.1 初始化/补建 `paper_state`（推荐）

```bash
python - <<'PY'
import os
from overleaf_ce_mcp.template import init_paper_state_workspace
res = init_paper_state_workspace(
    project_dir=os.environ["PROJ_DIR"],
    title="Your Manuscript Title",
    authors="First Author, Second Author",
    corresponding_email="author@example.com",
    keywords="ocean engineering, pinn",
    force=False,
)
print("created:", len(res["created_files"]))
print("skipped:", len(res["skipped_files"]))
print("paper_state_root:", res["paper_state_root"])
PY
```

## 4. 打包预演（不上传）

```bash
python - <<'PY'
import os
from overleaf_ce_mcp.upload import package_project_for_upload
res = package_project_for_upload(project_dir=os.environ["PROJ_DIR"])
print("zip:", res["zip_path"])
print("included:", res["included_count"], "skipped:", res["skipped_count"])
print("included_sample:", res["included_sample"][:10])
PY
```

## 5. 新建项目上传（首次）

```bash
python - <<'PY'
import os
from overleaf_ce_mcp.upload import package_project_for_upload, upload_zip_as_new_project
pack = package_project_for_upload(project_dir=os.environ["PROJ_DIR"])
up = upload_zip_as_new_project(
    ce_url=os.environ["CE_URL"],
    store_path=os.environ["AUTH_FILE"],
    zip_path=pack["zip_path"],
)
print(up)
PY
```

## 6. 更新已有项目（推荐：merge）

```bash
python - <<'PY'
import os
import tempfile, zipfile, shutil
from pathlib import Path
from overleaf_ce_mcp.upload import package_project_for_upload
from overleaf_ce_mcp.sync import ols_sync

pack = package_project_for_upload(project_dir=os.environ["PROJ_DIR"])
stage_root = Path(tempfile.mkdtemp(prefix="oe-stage-")).resolve()
stage_dir = stage_root / os.environ["PROJ_NAME"]
stage_dir.mkdir(parents=True, exist_ok=True)
with zipfile.ZipFile(pack["zip_path"], "r") as zf:
    zf.extractall(stage_dir)

(stage_dir / ".olignore").write_text(
    "*.aux\n*.bbl\n*.blg\n*.fdb_latexmk\n*.fls\n*.log\n*.out\n*.spl\n*.synctex.gz\n*.pdf\n*.zip\n",
    encoding="utf-8",
)

code, out, err = ols_sync(
    workspace_path=str(stage_dir),
    mode="local-only",
    project_name=os.environ["PROJ_NAME"],
    ce_url=os.environ["CE_URL"],
    store_path=os.environ["AUTH_FILE"],
    olignore=str(stage_dir / ".olignore"),
    delete_policy="i",  # merge: 忽略远端多余文件
    verbose=False,
)
print("exit_code:", code)
print(out[-1200:])
print(err[-600:])
shutil.rmtree(stage_root, ignore_errors=True)
PY
```

## 7. 覆盖已有项目（replace）

```bash
python - <<'PY'
import os
import tempfile, zipfile, shutil
from pathlib import Path
from overleaf_ce_mcp.upload import package_project_for_upload
from overleaf_ce_mcp.sync import ols_sync

pack = package_project_for_upload(project_dir=os.environ["PROJ_DIR"])
stage_root = Path(tempfile.mkdtemp(prefix="oe-stage-")).resolve()
stage_dir = stage_root / os.environ["PROJ_NAME"]
stage_dir.mkdir(parents=True, exist_ok=True)
with zipfile.ZipFile(pack["zip_path"], "r") as zf:
    zf.extractall(stage_dir)

(stage_dir / ".olignore").write_text(
    "*.aux\n*.bbl\n*.blg\n*.fdb_latexmk\n*.fls\n*.log\n*.out\n*.spl\n*.synctex.gz\n*.pdf\n*.zip\n",
    encoding="utf-8",
)

code, out, err = ols_sync(
    workspace_path=str(stage_dir),
    mode="local-only",
    project_name=os.environ["PROJ_NAME"],
    ce_url=os.environ["CE_URL"],
    store_path=os.environ["AUTH_FILE"],
    olignore=str(stage_dir / ".olignore"),
    delete_policy="d",  # replace: 删除远端多余文件
    verbose=False,
)
print("exit_code:", code)
print(out[-1200:])
print(err[-600:])
shutil.rmtree(stage_root, ignore_errors=True)
PY
```

## 8. 编译健康检查

```bash
python - <<'PY'
import os
from overleaf_ce_mcp.upload import health_check_project
res = health_check_project(
    ce_url=os.environ["CE_URL"],
    store_path=os.environ["AUTH_FILE"],
    project_name=os.environ["PROJ_NAME"],
    compile_check=True,
)
print(res)
PY
```

## 9. 学术检索（相关工作素材）

```bash
python - <<'PY'
import os
from overleaf_ce_mcp.scholar import build_related_work_pack
pack = build_related_work_pack(
    query="offshore structure wave load prediction physics-informed neural network",
    source="all",
    max_results_per_source=5,
    max_items_for_note=5,
    # 可不传：source=all 默认已走无 Key 来源
    s2_api_key=os.environ.get("S2_API_KEY"),
)
print("ok:", pack["ok"])
print("papers:", len(pack["papers"]))
print("note:")
print(pack["note_markdown"][:800])
print("\\n--- BibTeX sample ---")
for b in pack["bibtex_entries"][:2]:
    print(b)
    print()
PY
```

## 9.1 投稿期刊优选与引用核验

```bash
python - <<'PY'
from overleaf_ce_mcp.scholar import list_journal_presets, recommend_target_journals, verify_reference

print("== presets ==")
presets = list_journal_presets()
for x in presets["presets"]:
    print(x["key"], "->", x["name"])

print("\n== recommended journals ==")
rec = recommend_target_journals(
    topic="offshore structure wave load prediction with physics-informed deep learning",
    target_preference="any",
    max_candidates=5,
)
for j in rec["recommendations"]:
    print(j["preset_key"], j["score"], j["matched_papers"], j["sample_venues"][:2])

print("\n== verify one reference ==")
ref = verify_reference(
    title="Attention Is All You Need",
    year=2017,
)
print(ref["verdict"], ref["confidence"])
print((ref.get("corrected_bibtex") or "")[:300])
PY
```

## 9.2 LetPub 期刊信息抓取（可选）

```bash
python - <<'PY'
from overleaf_ce_mcp.scholar import letpub_search_journals, letpub_get_journal_detail

res = letpub_search_journals(searchname="Ocean Engineering", max_items=5)
print("count:", res["count"])
for j in res["journals"]:
    print(j["journalid"], j["name"], j["impact_factor"], j["is_oa"])

if res["journals"]:
    jid = res["journals"][0]["journalid"]
    detail = letpub_get_journal_detail(journalid=jid)
    d = detail["detail"]
    print("journal:", d["journal_name"])
    print("IF:", d["impact_factor_latest"])
    print("投稿网址:", d["submission_url"])
    print("审稿速度:", d["review_speed"])
PY
```

## 10. 生成多组深度研究提示词（R1）+ 导入报告

```bash
python - <<'PY'
from overleaf_ce_mcp.deep_research import generate_deep_research_prompt_set
res = generate_deep_research_prompt_set(
    topic="offshore structure wave load prediction",
    known_data="已有规则波与不规则波实验，含6种工况，指标为RMSE/MAE/R2",
    writing_direction="突出physics-informed约束在小样本工况下的泛化优势，并与纯数据驱动模型对比",
    baseline_models=["LSTM baseline", "Transformer baseline"],
    improvement_modules=["Physics Constraint Module", "Fusion Block"],
    target_journal="Ocean Engineering",
    draft_ideas="希望强调工程可部署性与极端工况鲁棒性",
    round_stage="r1",
    num_prompts=3,
)
for p in res["prompts"]:
    print("==", p["id"], p["focus"], "==")
    print(p["prompt"][:600], "...\n")
PY
```

将 GPT 网页版深度研究报告保存为 `/root/overleaf-workspace/test/deep-research-report.md` 后执行：

```bash
python - <<'PY'
from overleaf_ce_mcp.deep_research import ingest_deep_research_report
res = ingest_deep_research_report(
    report_file_path="/root/overleaf-workspace/test/deep-research-report.md",
    focus_topic="offshore wave load prediction",
    max_items=40,
)
print("dois:", len(res["dois"]))
print("arxiv_ids:", len(res["arxiv_ids"]))
print("bibtex_entries:", len(res["bibtex_entries"]))
print(res["quick_note"])
PY
```

## 10.1 综合题目/创新点/写作侧重点

```bash
python - <<'PY'
from overleaf_ce_mcp.deep_research import synthesize_paper_strategy
res = synthesize_paper_strategy(
    topic="offshore structure wave load prediction",
    target_journal="Ocean Engineering",
    baseline_models=["LSTM baseline", "Transformer baseline"],
    improvement_modules=["Physics Constraint Module", "Fusion Block"],
    key_results="在6种工况上，相对最佳baseline平均RMSE下降12.8%，MAE下降10.3%。",
    report_summaries=["R1指出小样本工况泛化是主要卖点，建议补强极端工况讨论。"],
)
print("titles:")
for t in res["recommended_titles"]:
    print("-", t)
print("\\ninnovation:")
for x in res["innovation_points"]:
    print("-", x)
print("\\nwriting_focus:")
for x in res["writing_focus"]:
    print("-", x)
PY
```

## 10.2 一键执行论文循环（推荐）

```bash
python - <<'PY'
from overleaf_ce_mcp.workflow import run_paper_cycle
res = run_paper_cycle(
    project_dir="/root/overleaf-workspace/test",
    day="2026-03-01",
    weekly_mode="auto",   # auto=仅周五生成周报；always=每次都生成；never=不生成
    run_loop=True,
    run_daily=True,
    auto_scan_inputs=True,        # 自动从 paper_state/inputs 补齐参数
    write_missing_checklist=True, # 缺失项输出到 paper_state/inputs/INPUT_MISSING.md
    strict_missing=False,         # 若设为 True，有缺失则直接停止
)
print("ok:", res["ok"])
print("steps:", res["executed_steps"])
print("summary:", res["paths"]["summary_json"])
PY
```

```bash
python - <<'PY'
from overleaf_ce_mcp.upgrade_loop import run_priority_upgrade_loop
res = run_priority_upgrade_loop(
    project_dir="/root/overleaf-workspace/test",
    max_tasks=6,
    dry_run=True,   # 先看优先级计划
    resume=True,
)
print("ok:", res["ok"])
for x in res["executed"]:
    print(x["task_id"], x["status"], x.get("priority_score"))
print("state:", res["state_path"])
PY
```

```bash
python - <<'PY'
import pathlib
from overleaf_ce_mcp.generic_priority_loop import (
    init_generic_priority_plan,
    list_generic_priority_plan_templates,
    run_generic_priority_loop,
)

ws = pathlib.Path("/root/overleaf-workspace/test").resolve()
print(list_generic_priority_plan_templates()["templates"])
init_generic_priority_plan(
    workspace_dir=str(ws),
    template_name="dev-feature-cycle",
    output_path="plan.json",
    force=True,
)
plan = ws / "plan.json"

res = run_generic_priority_loop(
    workspace_dir=str(ws),
    plan_path=str(plan),
    dry_run=True,
    allow_shell=False,
)
print(res["ok"], res["paths"]["run_json"])
PY
```

## 11. 初始化模型结构图生产包（Nano Banana Pro）

```bash
python - <<'PY'
from overleaf_ce_mcp.diagram_workflow import init_model_diagram_pack
res = init_model_diagram_pack(
    project_dir="/root/overleaf-workspace/test",
    model_name="Physics-Informed Wave-Load Predictor",
    drawio_file_path="/root/overleaf-workspace/test/figures/model-truth.drawio",
    truth_priority="drawio",
    modules=[
        "Input (Wave/Structure Features)",
        "Feature Encoder",
        "Physics Constraint Module",
        "Fusion Block",
        "Output (Load Prediction)",
    ],
)
print(res)
PY
```

生成后到以下目录查看：
- `/root/overleaf-workspace/test/figures/model-diagram`
- 重点核验：
  - `01-topology-truth.drawio`
  - `01-topology-lock.json`

## 12. 常用诊断命令

```bash
# 查看项目文件树（远端）
python - <<'PY'
import os
import pickle
from olsync.olclient import OverleafClient
auth = pickle.load(open("/root/.olauth", "rb"))
cli = OverleafClient(cookie=auth["cookie"], csrf=auth.get("csrf"), ce_url=os.environ["CE_URL"])
proj = cli.get_project(os.environ["PROJ_NAME"])
info = cli.get_project_infos(proj["id"])
root = info.get("project", info)["rootFolder"][0]
paths = []
def walk(folder, prefix=""):
    for d in folder.get("docs", []):
        paths.append(prefix + d["name"])
    for f in folder.get("fileRefs", []):
        paths.append(prefix + f["name"])
    for sub in folder.get("folders", []):
        walk(sub, prefix + sub["name"] + "/")
walk(root, "")
for p in sorted(paths):
    print(p)
PY
```

```bash
# 手动应用兼容补丁（通常不需要）
python - <<'PY'
from overleaf_ce_mcp.compat import ensure_compat_patches
print(ensure_compat_patches())
PY
```

## 13. 最小排错清单

- `projects: 0` 或直接异常：先检查 `.olauth` 是否过期。
- `compile status=failure`：看返回里的 `response_excerpt`，再看项目源文件。
- 同步后正文没上去：检查 `.olignore` 是否误包含 `sections/*` 或 `references.bib`。
- 更新错项目：确保 `PROJ_NAME` 与远端项目名完全一致。
